# Docker AMD ROCm GPU Requirements for Quantum Computing 101
# Purpose: AMD ROCm-accelerated packages
# Base Image: rocm/pytorch:latest
# Note: PyTorch with ROCm is already included in the base image
# Extends: base.txt

# Include base quantum computing requirements
-r base.txt

# GPU-accelerated Qiskit Aer simulator (will use available GPU backend)
qiskit-aer-gpu>=0.14.0

# AMD ROCm utilities
rocm-smi-lib>=0.1.0

# Machine learning tools
tensorboard>=2.15.0

# Cloud platform integration
qiskit-ibm-runtime>=0.18.0
qiskit-ibm-provider>=0.8.0
amazon-braket-sdk>=1.74.0
boto3>=1.34.0

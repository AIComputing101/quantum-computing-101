# Docker CPU-Only Requirements for Quantum Computing 101
# Purpose: CPU-specific packages (no GPU acceleration)
# Base Image: python:3.12-slim
# Note: PyTorch CPU version is already included in the base image
# Extends: base.txt

# Include base quantum computing requirements
-r base.txt

# CPU version of Qiskit Aer simulator
qiskit-aer>=0.14.0

# Machine learning tools
tensorboard>=2.15.0

# Cloud platform integration
qiskit-ibm-runtime>=0.18.0
qiskit-ibm-provider>=0.8.0
amazon-braket-sdk>=1.74.0
boto3>=1.34.0
